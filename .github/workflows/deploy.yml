name: Deploy Hugo Site

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    # 1️ 签出仓库代码
    - name: Checkout repository
      uses: actions/checkout@v3
      with:
        submodules: true

    # 2️ 安装 Hugo
    - name: Setup Hugo
      uses: peaceiris/actions-hugo@v2
      with:
        hugo-version: 'latest'

    # 3️ 构建 Hugo 网站
    - name: Build Hugo site
      run: hugo --minify

    # 4 部署到 GitHub Pages（gh-pages 分支）
    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./public
        publish_branch: gh-pages       # 修改这里为 gh-pages
        user_name: "Sue886"
        user_email: "2174874197@qq.com"
